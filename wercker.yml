box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.19"
        theme: hugo-material-docs
        flags: --disableSitemap=true --buildDrafts=true

deploy:
  steps:

    - install-packages:
        packages: git ssh-client

    #- lukevivier/gh-pages@0.2.1:
    #    token: $GIT_TOKEN
    #    domain: pablodav.github.io
    #    basedir: public

    - keanemind/git-push:
       gh_oauth: $GIT_TOKEN
       repo: pablodav/pablodav.github.io
       branch: master
       basedir: public

