box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.19"
        theme: hugo-material-docs
        flags: --disableSitemap=true --buildDrafts=true

deploy:
  steps:

    - install-packages:
        packages: git ssh-client

    #- lukevivier/gh-pages@0.2.1:
    #    token: $GIT_TOKEN
    #    domain: pablodav.github.io
    #    basedir: public

    - ysqi/git-push@0.7.15:
       gh_oauth: $GIT_TOKEN
       gh_pages_domain: pablodav.github.io
       repo: pablodav/pablodav.github.io.git
       branch: master
       basedir: public

